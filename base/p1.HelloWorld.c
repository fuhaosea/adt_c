// This is the c program's HeadFile.He include some common functions.
#include <stdio.h>

// This is the program's main function.It's program 's entrance.

int main(void){ // format: functionType + functionName + argumentList +functionContent
    printf("\nHello,World!\n");//This is a output sentence .He will echo "Hello,World!"
    return 0;//This is end sentence.Befor that sentence 's sentence not be running .
}
// This program is Over.